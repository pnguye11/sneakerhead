<%= render 'form' %>
<div class="text-center edit-links">
  <%= link_to "Delete Shoe", shoe_path(@shoe), method: :delete, data: { confirm: "Are you sure you want to delete this shoe?" } %>
  |
  <%= link_to "cancel", root_path %>
</div>
